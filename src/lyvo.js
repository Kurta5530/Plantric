export class Lyvo {
    constructor() {
        this.options = {
            log: true,
        }
    }

    generateWorkflow(task) {

    }

    inference(prompt) {

    }

    execute(workflow, options) {
        this.options = options || this.options;

    }
}